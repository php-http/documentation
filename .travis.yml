sudo: false

language: python

python:
    - 3.7

addons:
    apt_packages:
        - enchant

install:
    - pip install pipenv
    - pipenv install

script:
    - make SPHINXOPTS='-nW' html
    - make spelling
